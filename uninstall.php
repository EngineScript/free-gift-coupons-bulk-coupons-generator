<?php
/**
 * Uninstall script for Free Gift Coupons Bulk Coupon Generator.
 *
 * The plugin does not store persistent settings. Generated coupons are left in
 * place because they may still be active on the site.
 *
 * @package FreeGiftCouponsBulkGenerator
 */

// Prevent direct access.
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}

// No cleanup is currently needed.
